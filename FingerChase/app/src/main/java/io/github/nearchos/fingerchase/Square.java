package io.github.nearchos.fingerchase;

public class Square extends Rectangle {

    public Square(float cx, float cy, int side, float canvasWidth, float canvasHeight) {
        super(cx, cy, side, side, canvasWidth, canvasHeight);
    }
}